<!DOCTYPE html>
<html>

<head>
  <%- include('header.ejs') %>
</head>

<body>
          <%if (user) { %>             <%- include('navbar.ejs') %>         <% } else { %>             <%- include('navbar1.ejs') %>         <% } %>

    <div class="container text-left" id="no_results">

    <h2>Wyniki wyszukiwania dla frazy <em><%= what %></em>: </h2>
      <hr>
     <h3> Brak wyników dla szukanej frazy.... :(</h3> 
     <br> <br> <br>
     <h4> Houston, mamy problem? To nic! Spróbój jeszcze raz!</h4>
     
     <a class="btn btn-outline-info" role="button" href="/catalog">
        <i class="fa fa-arrow-left"></i >Wróć do katalogu
      </a>
      <br> <br> <br>
      <br> <br> <br>
      <br> <br> <br>
     <%if (booksArr !== undefined && booksArr !== null) { %>
      <hr>
      <h3 id="naj" > Najwyżej oceniane:</h3>
      <div class="books_container">

        <div class="card-deck-wrapper">
          <div class="card-deck">
            <% booksArr.forEach( (book) => { %>

              <div class="card text-center">
                <br>
                <img class="card-img-top img-fluid mx-auto d-block" src="/<%= book.image %>" alt="<%= book.title + " " + ' image' %>" >
                <div class="card-body d-flex flex-column">
                  <p class="card-text"><strong><i><%= book.title %></i></strong> <br>
                    <%= book.author %></p>
                  <a href="<%= '/book_profile/'+ book.book_id %>" class="btn btn-info btn-block mt-auto"> Więcej </a>
                </div>
              </div>

              <% }) %>
          </div>
        </div>

      </div>

      <% } %>
  
    </div>
    <%- include('footer.ejs') %>
</body>

</html>